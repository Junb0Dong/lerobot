#!/usr/bin/env bash
# Default DLC entry: CloseDrawer tokenizer + semantic policy smoke pipeline.
# shellcheck disable=SC1091
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/cmd_robocasa_closedrawer_pipeline.sh"
